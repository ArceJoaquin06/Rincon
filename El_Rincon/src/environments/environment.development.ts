export const environment = {
    production: false,
    apiUrl: "http://localhost:13847/" //Tiene que coincidir con el puerto en donde corro, si uso una api real
     //Tiene que ser el local host del Swagger
};